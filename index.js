function multiplyNumbers(num1, num2) {
  return num1 * num2;
}

let result = multiplyNumbers(10, 10);
console.log(result);

const firstName = "Rachel",
  lastName = "Opuba",
  stuternTrack = "Frontend",
  skills = "HTML",
  favColor = "blue",
  independenceYear = 1960;

const sentence = `${firstName} ${lastName} is a ${stuternTrack} developer with skills in ${skills} . ${firstName}'s country gained independence in ${independenceYear} and she loves ${favColor} color`;

console.log(sentence);
